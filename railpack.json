{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "mkdir -p .data && AGENT_REACH_DATA_DIR=.data python3 scripts/crypto_signal.py watchlist --preset scalping --interval 5m --mtf --smart-telegram --watch --every 120"
  }
}
